namespace WhatsAppMonitorAssistant.Contract.Tests;

/// <summary>
/// The Meta transport contract suite arrives with Issue #13. It stays a skipped placeholder so the
/// remaining contract work is visible without pretending a test exists.
/// </summary>
public sealed class MetaContractPlaceholderTests
{
    [Fact(Skip = "Meta webhook/outbound contract fixtures arrive with Issue #13.")]
    public void Meta_contract_suite_is_not_implemented_yet()
    {
        Assert.Fail("Placeholder - the Meta contract suite arrives with Issue #13.");
    }
}
